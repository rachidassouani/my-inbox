version: '3'

services:
  cassandra:
    image: cassandra:latest
    container_name: myinbox-cassandra
    ports:
      - "9042:9042"
    volumes:
      - cassandra-data:/var/lib/cassandra

volumes:
  cassandra-data:
    driver: local